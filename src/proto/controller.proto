syntax = "proto3";

service ControllerMessagingService {
  rpc SendNeighborsASN(ASList) returns (ResponseStatus) {}
}

message ASList {
  uint32 local_as = 1;
  repeated uint32 remote_as_list = 2;
}

message ResponseStatus {
  string status = 1;
}
